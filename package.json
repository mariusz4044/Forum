{
  "name": "project-root",
  "private": true,
  "scripts": {
    "start": "npx npm-run-all --parallel start:backend start:frontend",
    "start:backend": "cd backend && npm run ts",
    "start:frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
